  <div class="mb-6">
    <%= form_with model: category, class: "flex gap-2" do |f| %>
      <%= f.text_field :name, placeholder: "เพิ่มหมวดใหม่",
          class: "flex-1 border-2 border-indigo-300 rounded px-3 py-2 focus:outline-none focus:ring-2 focus:ring-pink-400",maxlength: 40 %>
      <%= f.submit "➕ เพิ่ม",
          class: "px-5 py-2 bg-green-500 text-white font-semibold rounded hover:bg-pink-600 transition" %>
    <% end %>
  </div>