<%= link_to categories_path(category_id: category.id),
          class: "rounded-full w-full line-clamp-2 px-4 py-2 border #{params[:category_id].to_i == category.id ? 'bg-indigo-600 text-white' : 'bg-white text-indigo-600'}" do %>
  <%= category_name_with_count(category)  %>
<% end %>